name: Gerar APK - Gerador QR Chamados

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - 'saida/**'

permissions:
  contents: write

jobs:
  gerar-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Baixar projeto
        uses: actions/checkout@v4

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.9.0'

      - name: Preparar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Preparar Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar Android API 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Instalar Cordova
        run: npm install -g cordova@13.0.0

      - name: Instalar dependências
        run: npm install

      - name: Preparar bibliotecas offline
        shell: bash
        run: |
          mkdir -p www/js
          curl -L --fail --retry 3 "https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js" -o www/js/xlsx.full.min.js
          curl -L --fail --retry 3 "https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js" -o www/js/qrcode.min.js
          curl -L --fail --retry 3 "https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js" -o www/js/html2canvas.min.js
          curl -L --fail --retry 3 "https://cdn.jsdelivr.net/npm/jspdf@2.5.2/dist/jspdf.umd.min.js" -o www/js/jspdf.umd.min.js
          test -s www/js/xlsx.full.min.js
          test -s www/js/qrcode.min.js
          test -s www/js/html2canvas.min.js
          test -s www/js/jspdf.umd.min.js

      - name: Recriar Android e plugins
        shell: bash
        run: |
          rm -rf platforms plugins
          cordova platform add android@15.0.0
          cordova prepare android

      - name: Gerar APK
        run: cordova build android --debug

      - name: Preparar APK
        shell: bash
        run: |
          mkdir -p saida
          cp platforms/android/app/build/outputs/apk/debug/app-debug.apk saida/Gerador_QR_Chamados.apk
          ls -lh saida/Gerador_QR_Chamados.apk

      - name: Salvar APK na pasta saida da main
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f saida/Gerador_QR_Chamados.apk www/js/*.js
          git diff --cached --quiet || git commit -m "Atualizar APK do Gerador QR Chamados"
          git push

      - name: Publicar APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: Gerador-QR-Chamados-APK
          path: saida/Gerador_QR_Chamados.apk
          if-no-files-found: error
          retention-days: 14
          compression-level: 0

      - name: Publicar estrutura Cordova gerada
        uses: actions/upload-artifact@v4
        with:
          name: Estrutura-Cordova-Gerada
          path: |
            plugins/
            platforms/android/
            saida/Gerador_QR_Chamados.apk
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
